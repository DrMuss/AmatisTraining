using Microsoft.AspNetCore.Identity;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;
using AT.Data.Identity;

namespace AT.Helpers
{
    public static class UserManagerExtensions
    {
        /*
        public static ApplicationUser FindByAuthyID(this UserManager<ApplicationUser> um, string authyId)
        {
            return  um?.Users?.SingleOrDefault(x => x.AuthyId == authyId);
        }
        */
        
    }
}
